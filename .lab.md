# Lab Configuration

> Auto-generated by /lab-create on 2026-08-12

## Project

**Name:** vibeusage
**Type:** Go
**Description:** CLI tool to track AI coding assistant usage and quotas across multiple providers.

## Commands

### Testing
```bash
just test
```

### Linting
```bash
just lint
```

### Type Checking / Static Analysis
```bash
just vet
```

### Vulnerability Check
```bash
just vuln
```

### Full Verification
```bash
just check
```

### Build
```bash
just build
```

### Dev / Run
```bash
just run
```

## Structure

```
cmd/         — CLI entry point (cmd/vibeusage)
internal/    — Core implementation packages (auth, catalog, cli, display, fetch, history, provider, routing, updater, etc.)
docs/        — Documentation
scripts/     — Helper scripts (e.g. screenshots.sh)
```

## Git Workflow

> Read by /lab-setup (to open a branch/worktree) and /lab-end (to merge and clean up).

**Git root:** /Users/zt_mini/zt_dev/vibeusage
**Monorepo:** false
**App path:** (none)
**Base branch:** main
**Branch prefix:** fix/vibeusage
**Merge strategy:** squash-merge to base
**Worktree root:** /Users/zt_mini/zt_dev/vibeusage/.worktrees
**Cleanup policy:** delete feature branch + worktree after successful merge

## MCP Tools

- `warp_run_command`: Execute shell commands, read/write/edit files, search documentation
- `agent-browser`: Web automation and browser testing

## Verification Notes

- Run `just check` for comprehensive local verification (tests, linting, vet, vuln checks, module tidiness).
- Local sandbox development uses `.sandbox/vibeusage` loaded via direnv or env vars (`VIBEUSAGE_CONFIG_DIR`, `VIBEUSAGE_DATA_DIR`, `VIBEUSAGE_CACHE_DIR`).

## Known Issues

- None noted

## Session Log

### 2026-09-24 (lint tooling)
**Goal:** Install the missing local dev tooling (`just`, `golangci-lint`, `pre-commit`) so `just lint`/`just check` can actually run, then fix whatever they find.
**Branch:** fix/vibeusage/lint-clean (squash-merged)
**Done:**
- Installed `just` 1.58.0, `golangci-lint` 2.14.0, and `pre-commit` 4.6.2 via Homebrew. `golangci-lint`'s formula pulled in an upgrade of the `go` dependency itself (1.26.5 → 1.27.1); confirmed compatible (`go.mod` declares `go 1.26.5` with no `toolchain` pin) and, as a side effect, this cleared the 6 stdlib CVEs the previous session's `govulncheck` run had flagged.
- First-ever `just lint` run surfaced real, pre-existing issues (never checked before, since none of these binaries existed locally): 4 files with extra trailing blank lines at EOF, 1 misordered import (`internal/cli/root.go`), and 6 golangci-lint findings (1 errcheck, 1 govet, 4 staticcheck) — plus 2 outdated GitHub Action SHA pins from the `check` (gh-actionkit) hook.
- Two of the pre-commit hooks (`end-of-file-fixer`, `go-fmt`-via-`gofmt -w`) auto-modify files in place; the first exploratory `just lint` run was on `main` before this session's branch existed, so I reverted those incidental edits immediately (`git checkout --`) rather than leave unreviewed auto-fixes sitting on the base branch, then redid them properly on `fix/vibeusage/lint-clean`.
- Discovered golangci-lint's default `max-same-issues: 3` cap (no repo override in `.golangci.yml`) was hiding half the real ST1005 findings — the first pass showed 3, but `golangci-lint run --max-same-issues=0 --max-issues-per-linter=0 ./...` revealed 6 total (2 more capitalized-error-string instances in `browsercookies_darwin.go`, 2 in `response.go`). Fixed all 6, then re-ran uncapped again and confirmed 0 issues repo-wide — not just that the originally-reported ones were gone.
- Fetched authoritative full commit SHAs for the 2 outdated Action pins via `git ls-remote --tags` rather than trust a terminal-wrapped table, then applied them.
**Verification:** `just check` (`go test ./... -race` full suite, `pre-commit run --all-files` i.e. `just lint`, `go vet ./...`, `go tool govulncheck ./...`, `go mod tidy -diff`) passes clean end-to-end on the merged `main` — the first time this repo's full check suite has ever actually run to completion. Also spot-verified `golangci-lint run --max-same-issues=0 --max-issues-per-linter=0 ./...` → `0 issues.` directly, and `go clean -testcache` + targeted re-run to confirm the `reflect.Pointer` edit was genuinely exercised, not a stale cache hit.
**Merged:** yes — squash-merged to `main`, branch `fix/vibeusage/lint-clean` deleted (`git branch -D`, squash-merges aren't detected as merged by `-d`)
**Open items:** None from this task.

### 2026-09-24 (fix)
**Goal:** Implement the follow-up TODO from the diagnostic session below: stop a stale Claude throttle marker from outliving a successful re-authentication.
**Branch:** fix/vibeusage/clear-throttle-on-reauth (squash-merged)
**Done:**
- `enableProvider` in `internal/cli/auth.go` now calls `config.ClearThrottle(providerID)` after enabling a provider, so every successful `auth` path (manual key entry, confirming reuse of detected credentials, device/custom flows, `--token`) drops any stale rate-limit cooldown for that provider.
- Deliberately narrower than `config.ClearProviderCache`: leaves the cached snapshot and org ID in place as a fallback, only clears the throttle marker that re-authenticating actually invalidates.
- Added `TestEnableProvider_ClearsStaleThrottleMarker`, `TestAuthClaude_ManualKeyEntry_ClearsStaleThrottleMarker`, and `TestAuthProvider_ReusingDetectedCredentials_ClearsStaleThrottleMarker` to `internal/cli/auth_test.go`.
- Manually reproduced the reported bug end-to-end with a throwaway binary and sandboxed `VIBEUSAGE_*_DIR`: seeded a future-dated throttle marker, confirmed `vibeusage usage claude` returns the cached "Rate limited by Anthropic; retry after ..." error without a live call, ran `vibeusage auth claude --token ...`, and confirmed the marker file is gone afterward.
**Verification:** `go build ./...`, `go vet ./...`, `go test ./... -race` (all packages, full suite) all pass on the merged `main`; `gofmt -l` clean on touched files (5 unrelated files elsewhere in the tree have pre-existing drift, confirmed present at the prior commit too); `go mod tidy -diff` clean. `just lint` (`pre-commit`/`golangci-lint`) could not be run — neither binary is installed in this environment.
**Merged:** yes — squash-merged to `main`, branch `fix/vibeusage/clear-throttle-on-reauth` deleted (`git branch -D`, since squash-merges aren't detected as merged by `-d`)
**Open items:** None from this task. `just lint`/`golangci-lint` remains unverified locally; worth running in CI or installing the tool before the next session touches lint-sensitive code.

### 2026-09-24
**Goal:** Diagnose user-reported "Claude: Rate limited by Anthropic" errors surfacing from `vibeusage usage`.
**Branch:** main
**Done:**
- Traced the error to `OAuthStrategy`/`WebStrategy` 429 handling in `internal/provider/claude/oauth.go` and `internal/provider/claude/web.go`, which persist a `ThrottleMarker` via `internal/fetch/pipeline.go`.
- Found the root cause of "still happening after re-login": a stale throttle marker cached at `~/Library/Caches/vibeusage/throttles/claude.json` short-circuits `pipeline.go`'s fetch (serves the cached error) before any live network call is attempted, regardless of newly refreshed credentials.
- Cleared the user's stale throttle marker to unblock them immediately.
- No application code was changed; this was a read-only diagnostic session.
**Verification:** N/A — docs-only session log/TODO update; no application code changed this session (diagnostic investigation only, confirmed via `git status --short` clean on `main`).
**Merged:** yes (working directly on base branch `main`; no feature branch was created)
**Open items:** Consider making the throttle marker re-login-aware — e.g. clear/bypass `Throttles` on `vibeusage auth claude` (mirroring the existing `ClearProviderCache` call in `internal/cli/auth.go`) so a fresh login doesn't keep surfacing a pre-login 429 until its stale `retry_at` elapses. See `internal/config/cache.go` and `internal/fetch/pipeline.go`.

### 2026-08-18
**Goal:** Investigate Model Studio authentication lifecycle, automate cookie refresh strategies, and verify provider health.
**Branch:** main
**Done:**
- Diagnosed Model Studio API authentication failures (`ConsoleNeedLogin`) and verified browser cookie Keychain decryption.
- Analyzed Alibaba Cloud Model Studio session expiration constraints and evaluated automated session management options.
- Verified all unit and integration test suites across providers.
**Verification:** `go test ./...` and `go vet ./...` passed cleanly across all packages.
**Merged:** yes (working directly on base branch `main`)
**Open items:** Add CLI commands and integration for ModelStudio provider.

### 2026-08-13
**Goal:** Support AGY CLI quota integration, fix Codex window classification & Grok OAuth device flow, implement ModelStudio provider, refactor Claude OAuth flow, and fix overdue reset timestamp calculations.
**Done:**
- Added `AGYCLIStrategy` to `antigravity.go` parsing `agy -p "/usage" --output-format json` with weekly & 5h session model group breakdowns.
- Fixed Codex window classification in `codex.go` to display 7-day secondary windows as `Weekly`.
- Fixed Grok device auth flow in `grok.go` and `device/run.go` by including `grant_type: "urn:ietf:params:oauth:grant-type:device_code"`.
- Implemented ModelStudio provider (`internal/provider/modelstudio/`) featuring browser cookie authentication (with macOS Keychain decryption), API response parsing, and comprehensive test coverage.
- Refactored Claude OAuth flow (`internal/provider/claude/oauth.go`) and updated authentication documentation in `README.md`.
- Enhanced `models.ParseRFC3339Ptr` to support fractional seconds and RFC3339Nano / ISO8601 variations.
- Added `models.UsagePeriod.EffectiveResetTime()` and updated `TimeUntilReset()` to dynamically roll expired timestamps forward to the next period boundary, fixing "reset overdue" for Claude and Antigravity.
- Added unit test suites across `antigravity`, `codex`, `grok`, `claude`, `models`, and `modelstudio` packages.
**Verification:** `go test ./internal/models ./internal/provider/claude ./internal/provider/antigravity` passed cleanly, and `vibeusage usage` CLI output verified active reset countdown timers.
**Open items:** Add CLI command integration for ModelStudio provider.
